import Testing
@testable import TilerCore

// Smoke test proving the test target links against TilerCore.
// Replaced by real recognizer tests in Phase 2.
@Test func testInfrastructureRuns() {
    #expect(Bool(true))
}
