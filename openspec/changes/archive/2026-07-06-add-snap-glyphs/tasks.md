# Tasks — add-snap-glyphs
- [x] 1.1 SnapGlyph (Canvas, static) for all placements + next-display + lock.
- [x] 1.2 Lead column in both cheat-sheet grids; rows carry a glyph kind.
- [x] 1.3 **[USER GATE PASSED]** owner iterated the design as rendered rasters
      (picture-first): window style + dots, no tiling arrows, centered center-third,
      restore = bold ccw arrow encircling a centered window, next-display = target
      front + dim dashed source. Ported to SnapGlyph via CoreGraphics NSImage (fixed
      colors, resolution-independent). Screenshots regenerated; merged; archived;
      released v0.2.4.
