# settings — delta for split-hotkey-groups

## MODIFIED Requirements

#### Requirement: Settings window (toggle split)

The single hotkeys toggle SHALL be replaced by two: "Window tiling hotkeys"
(default off) and "Lock screen hotkey ⌃A" (default on). Both persist and apply
immediately.
