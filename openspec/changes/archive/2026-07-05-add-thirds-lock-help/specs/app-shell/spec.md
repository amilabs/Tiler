# app-shell — delta for add-thirds-lock-help

## MODIFIED Requirements

#### Requirement: Menu-bar-only application (label change)

The first menu item SHALL be labeled **Help** (was "Tiler"), still opening the
unified window. The cheat sheet SHALL include the new bindings: ⌃A lock screen,
and ⇧ / ⇧⌘ swipe variants for thirds.
