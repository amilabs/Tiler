# Current specs

This folder holds the merged, currently-true capability specs. It is empty until the first
change is archived; requirement deltas live in `openspec/changes/<change-id>/specs/` while
a change is in flight.
